- [x] 全局暗黑模式已被彻底移除，任何页面均无残留的深色背景（如 `#141413`, `#1c1c1a`）。
- [x] 字体栈已重置为现代无衬线字体，字号层级与对比度符合易读性最佳实践。
- [x] `App.vue` 侧边栏和主内容区采用了浅色主题（如浅灰色大背景、白色侧边栏），并伴有恰当的阴影与圆角（至少 `rounded-xl` 或 `rounded-2xl`）。
- [x] 导航菜单具备清晰的选中和悬浮反馈，避免刺眼的粗暴高亮。
- [x] `MemberList.vue`（帮众管理）页面表格和按钮已使用极简明亮风格重构，呈现出干净的白底卡片容器效果。
- [x] `TeamBuilder.vue`（团队排表）页面的拖拽容器、团队卡片、待选区卡片采用了统一的白底或浅灰圆角卡片设计。
- [x] `DataAnalysis.vue`（数据分析）中的 ECharts 图表配色方案已适配浅色背景，不再有暗色背景、粗糙的网格线或对比度差的文本。
- [x] 界面具有高完成度（Production-grade），排版呼吸感强，元素对齐精准，无 generic AI 审美（如无意义的默认渐变紫）。